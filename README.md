# How to run
1. Copy `.env.example` to `.env`. Tweak passwords if need be
2. `docker compose up -d` to start
3. Visit `localhost:8501` for Streamlit and type a message
   - A message should be soon returned from a mock model